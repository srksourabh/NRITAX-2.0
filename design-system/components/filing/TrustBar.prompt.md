Certification marks, in exactly three places: the marketing footer, the first upload screen, and the payment screen.

```jsx
<TrustBar marks={[
  { name: 'e-Return Intermediary', reference: 'ERIP00XXXX' },
  { name: 'ISO/IEC 27001:2022', reference: 'Cert. XXXXXX' },
  { name: 'AES-256 at rest' },
]} />
```

Never animated, never a carousel, never a mark the company does not hold. Where a registration number exists it is shown. No logo files were supplied for these marks, so they render as type lockups — swap in the real artwork when available.
