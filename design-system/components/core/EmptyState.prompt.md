The blank state for any list, schedule or upload area.

```jsx
<EmptyState line="No capital gains entries yet. Add one, or upload your broker statement."
  action={<Button variant="secondary">Add an entry</Button>} />
```

One line, one action. Never an illustration or a mascot.
