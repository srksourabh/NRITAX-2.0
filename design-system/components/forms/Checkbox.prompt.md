Independent choices and declarations.

```jsx
<Checkbox label="I held foreign assets during the year" hint="Schedule FA becomes applicable" checked={v} onChange={setV} />
```

The label is clickable and the row is 44px tall. Never use a checkbox for a two-way setting that applies immediately — that is `Switch`.
