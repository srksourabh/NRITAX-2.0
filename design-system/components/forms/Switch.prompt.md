Settings that apply the moment they are flipped.

```jsx
<Switch label="Email me when the department processes this return" checked={v} onChange={setV} />
```

Uses `role="switch"` on a real checkbox. If the change needs a save action, use `Checkbox` inside the form instead.
