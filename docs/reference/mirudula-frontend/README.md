# Mirudula frontend foundation (reference)

Snapshot of the `MIRUDULA-MAIN` branch landing and onboarding work, kept for
provenance. The live marketing page on `main` is `web/src/components/landing/`,
which merges this structure with the NRITAX design system and the shipping
filing product.

Do not run this Vite app in production. It is source history only.
