/**
 * ITR-2 form schema. The wizard, the validator, the calculation engine and the
 * JSON builder all read the return's shape from here.
 */

export { ITR2_SCHEDULES, ITR2_OPTIONS, itr2Schedule } from '@/lib/itr/itr2/schedules';
